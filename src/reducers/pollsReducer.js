const poll = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

const polls = (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default polls;
